apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drawio-data
  namespace: drawio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn